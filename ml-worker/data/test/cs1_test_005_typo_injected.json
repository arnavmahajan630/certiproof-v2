{
  "answer_id": "cs1_test_005_typo_injected",
  "question_id": "cs1",
  "derived_from_train_id": "cs1_train_005",
  "variant_type": "typo_injected",
  "answer_text": "Recursion is a programming tcehnique where a function solves a prbolem by calling itself with a smaller or simpler version of the same problem, gradually working toward a case simple enough to answer directly. That simplest case is called the base case, and it's essential: without one, the function would keep calling itsefl indefinitely, which eventually crashes the progrma with a stack overflow once it runs out of call-stack memory. A classic example is computing a factorail: `factorial(n)` can be defined as `n * factorial(n - 1)`, with the base case `factorial(0) = 1` sotpping the chain of calls. Each rceursive call is placed on the call stack, and once the base case is reached, the results unwind back up through all the pending calsl, multiplying together as they go, utnil the original call returns the fianl answer. Recursion often makes code that operates on naturally recursive structures \u2014 like trees or nested lists \u2014 much cleaner to read than an equivalent loop-based version.",
  "human_reviewed": true,
  "ai_generated": true,
  "human_scores": {
    "cs1_c1": 2,
    "cs1_c2": 2
  },
  "length_bucket": "long",
  "data_source": "synthetic"
}